{% extends "layout.html.twig" %}

{% block page_header %}
  <div class="page-header" style="background: url({{ asset('assets/img/banner1.jpg') }});">
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <div class="breadcrumb-wrapper">
            <h2 class="product-title">{{ block('title') }}</h2>
            <ol class="breadcrumb">
              <li><a href="{{ path('homepage') }}"><i class="ti-home"></i> Inicio</a></li>
              <li class="current">{{ block('title') }}</li>
            </ol>
          </div>
        </div>
      </div>
    </div>
  </div>
{% endblock %}
